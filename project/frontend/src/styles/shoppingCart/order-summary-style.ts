import {css} from "lit";

export default css `
    .summary-wrapper {
        width: 350px;
        height: 350px;
        background: #FFFFFF;
        box-shadow: 0px 1px 4px rgba(0, 0, 0, 0.25);
        border-radius: 15px;
    }
`;